# Very Good Claude Marketplace

## Pre-commit checklist

Before committing, ensure every plugin in `.claude-plugin/marketplace.json` is listed in `README.md` with:

- A link to its GitHub repository (from the `repository` field in the marketplace entry)
- An accurate description
